# Environment
NODE_ENV=development

# Server
PORT=3000

# Database (로컬 Docker Compose PostgreSQL)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=bakmyunjun
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}

# Redis (로컬 Docker Compose Redis)
REDIS_URL=redis://localhost:6379

# JWT
# 최소 32자 이상의 랜덤 문자열을 사용하세요
JWT_ACCESS_SECRET=your-access-secret-key-minimum-32-characters
JWT_REFRESH_SECRET=your-refresh-secret-key-minimum-32-characters

# OAuth - GitHub
# GitHub Developer Settings에서 발급받은 Client ID와 Secret을 입력하세요
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# OAuth - Kakao
# Kakao Developers에서 발급받은 Client ID와 Secret을 입력하세요
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=

# AI (Upstage Solar)
UPSTAGE_API_KEY=

# Swagger
# production에서는 기본적으로 비활성화됩니다. (true/1로 켜기)
ENABLE_SWAGGER=
SWAGGER_BASIC_USER=
SWAGGER_BASIC_PASSWORD=

# Sentry
SENTRY_DSN=

# Production 환경 변수 (프로덕션 배포 시)
# CORS 허용 Origin 목록 (쉼표로 구분)
# FRONTEND_URL=https://your-frontend-domain.com,https://www.your-frontend-domain.com
