# Environment
NODE_ENV=production

# Server
PORT=3000

# Database (프로덕션 데이터베이스 - AWS RDS, Supabase, Neon 등)
# 실제 배포 시 플랫폼에서 제공하는 연결 URL로 변경하세요
DATABASE_URL=postgresql://username:password@production-db-host:5432/bakmyunjun_prod

# Redis (프로덕션 Redis - AWS ElastiCache, Upstash 등)
# 실제 배포 시 플랫폼에서 제공하는 연결 URL로 변경하세요
REDIS_URL=redis://production-redis-host:6379

# JWT (프로덕션용 - 반드시 개발 환경과 다른 값 사용)
JWT_ACCESS_SECRET=production-access-secret-key-minimum-64-characters-use-openssl-to-generate
JWT_REFRESH_SECRET=production-refresh-secret-key-minimum-64-characters-use-openssl-to-generate

# OAuth - GitHub (프로덕션 클라이언트 ID/Secret)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# OAuth - Kakao (프로덕션 클라이언트 ID/Secret)
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=

# AI (Upstage Solar)
UPSTAGE_API_KEY=

# CORS
FRONTEND_URL=https://your-frontend-domain.com

# Swagger (프로덕션 기본: 비활성화)
ENABLE_SWAGGER=

# Swagger Basic Auth (프로덕션에서 Swagger를 켤 때 필수)
SWAGGER_BASIC_USER=frontend
SWAGGER_BASIC_PASSWORD=change-me

# Sentry
SENTRY_DSN=
